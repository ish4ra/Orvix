name: Prepare Pikora v0.4.1 Source and Playback

on:
  push:
    branches:
      - v0.4-home-library
    paths:
      - 'tools/patch_v041.py'
      - '.github/workflows/prepare-v041.yml'

permissions:
  contents: write

jobs:
  patch-commit-validate:
    runs-on: windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          ref: v0.4-home-library

      - name: Apply v0.4.1 patch
        run: python tools/patch_v041.py

      - name: Remove temporary patch helpers
        shell: pwsh
        run: |
          Remove-Item tools/patch_v040.py -ErrorAction SilentlyContinue
          Remove-Item tools/patch_v041.py -ErrorAction SilentlyContinue
          Remove-Item .github/workflows/prepare-v040.yml -ErrorAction SilentlyContinue
          Remove-Item .github/workflows/prepare-v041.yml -ErrorAction SilentlyContinue

      - name: Commit source before validation
        shell: pwsh
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "feat: v0.4.1 source ranking and playback fixes"
          git push origin HEAD:v0.4-home-library

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Generate Windows runner
        run: flutter create --platforms=windows --project-name pikora .

      - name: Get dependencies
        run: flutter pub get

      - name: Analyze app sources
        run: flutter analyze lib --no-fatal-warnings --no-fatal-infos

      - name: Build Windows release
        run: flutter build windows --release
